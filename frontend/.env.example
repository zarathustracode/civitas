# Frontend environment. Copy to `.env` and fill in.

# Where the SvelteKit app proxies /api/* in development.
# Production deployments rewrite /api/* via reverse proxy and ignore this.
API_PROXY_TARGET=http://127.0.0.1:8080
